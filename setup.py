#!/usr/bin/env python

import setuptools

if __name__ == "__main__":
    setuptools.setup(
            name = "QuaTrEx",
            packages = setuptools.find_packages(),
            install_requires=[


            ])
